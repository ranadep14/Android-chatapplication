<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--<EditText
        android:id="@+id/edtTxt_ConvName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="28sp"
        android:hint="enter conversation name"
        android:layout_alignParentTop="true"/>

    <Spinner
        android:id="@+id/spinnerURM"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:spinnerMode="dropdown"
        android:layout_below="@id/edtTxt_ConvName"/>

    <Spinner
        android:id="@+id/contacts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:spinnerMode="dropdown"
        android:layout_below="@id/spinnerURM"/>-->

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rview_contacts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"/>

    <Button
        android:id="@+id/btnCreateConv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="28sp"
        android:textAllCaps="false"
        android:text="create conversation"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>